#include <stdio.h>
#include "main.h"

/**
 * _strcmp - compare two strings
 * @s1: description
 * @s2: string
 * @n: value
 * Return: (0)
 */

int _strcmp(char *s1, char *s2)
{
	int results;

	results = strcmp(s1, s2);
	return (results);
}
