#include <stdio.h>
#include "main.h"

/**
 * _strcmp - compare two strings
 * @s1: description
 * @s2: string
 * @n: value
 * Return: (0)
 */

int _strcmp(char *s1, char *s2)
{
	int i;
	int fire;

	for (i = 0; s1[i] != '\0' || s2[i] != '\0'; i++)
	{
		if (s1[i] != s2[i])
			break;
		if else(s1[i] == s2[i]
				continue;
	}
	return (0);
}
