#include <stdio.h>

/**
 * Main function - prints the number of arguments passed into it
 * @argc: count argument
 * @argv: agrgumets
 * main return: success
 * Return: 0
 */
int main (int argc, char *argv[])
{
	printf("\%s\n", argv[0];
	return (0);
}
