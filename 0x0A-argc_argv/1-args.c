#include <stdio.h>

/**
 * Main function - prints the number of arguments passed into it
 * @argc: count argument
 * @argv: agrgumets
 * main return: success
 * Return: 'main'
 */
int main (int argc, char *argv[])
{
	printf("%d\n", argc);
	return (0);
}
